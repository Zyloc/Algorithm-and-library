/*
 * Satyam Swarnkar (Zyloc), NIT Silchar
*/
#include <bits/stdc++.h>
using namespace std;
int main(){
	string orginal,changed;
	cin>>orginal>>changed;
	map<char,char> mp;
	map<char,bool> mapping;
	for(int i=0;i<orginal.length();i++){
		if(mapping[orginal[i]])
	}
	return 0;
}