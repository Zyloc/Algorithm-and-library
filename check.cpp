/*
 * Satyam Swarnkar (Zyloc), NIT Silchar
*/
#include <bits/stdc++.h>
#define lli long long int
using namespace std;
vector<vector<pair<bitset,int > > > dp;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	dp.clear();
	//dp.resize(100,vector<pair<bitset,int> >(100,{0,-1}));
	return 0;
}
